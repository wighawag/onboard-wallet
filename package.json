{
  "name": "onboard-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "An Iframe based Wallet",
  "devDependencies": {
    "set-defaults": "^0.0.2"
  },
  "scripts": {
    "prepare": "set-defaults .vscode",
    "start": "zellij --layout zellij.kdl a $npm_package_name || zellij --layout zellij.kdl -s $npm_package_name",
    "dev": "pnpm run --parallel -r dev"
  }
}
